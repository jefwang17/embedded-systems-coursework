#include <stdio.h>

int main(){

    int val1, val2;

    printf("Input starting number of range:");
    scanf("%d", &val1);
    printf("Input ending number of range :");
    scanf("%d", &val2);
    
    return 0;
}